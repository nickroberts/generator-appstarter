language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-appstarter' ]; then cd .. && eval "mv $currentfolder generator-appstarter" && cd generator-appstarter; fi

